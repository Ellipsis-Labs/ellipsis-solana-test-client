# Solana Test Client (Ellipsis fork)

This is a forked version of a number of modules from the core Solana repo that supports getting the sequence of executed instructions from confirmed transactions.
